function spawn_boss(_x, _y, _id, _force = false)
{
	/*
	if (!_force) && (instance_exists(obj_Boss)) exit;
	
	var _data = global.boss_data[_id];
	var _hp = _data.hp;
	
	with (instance_create_layer(_x, _y, "Instances", obj_Boss))
	{
		boss_id = _id;
		sprite_index = _data.sprite;
		
		entity_init(, _hp);
		
		timer = 0;
		state = 0;

		dead = false;
		explosion = [];

		xdirection = 0;

		handle = undefined;
	}
	*/
}